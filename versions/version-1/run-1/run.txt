from ultralytics import YOLO

# 1. Load your YOLO26 Medium model
model = YOLO('yolo11m.pt')

# 2. Train the model with the MuSGD optimizer
results = model.train(
    data=f"{dataset.location}/data.yaml",
    epochs=50,
    imgsz=640,
    plots=True,
    device=0,
    batch=32,
    optimizer='MuSGD'
)